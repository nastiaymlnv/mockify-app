export enum AuthActionType {
	LOGIN = 'authStore/login',
	GET_AUTH_USER = 'authStore/getCurrentUser',
}

export enum ProductsActionType {
	GET_PAGINATED_PRODUCTS = 'productsStore/getPaginatedProducts',
}
